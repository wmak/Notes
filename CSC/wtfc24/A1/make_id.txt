In Java rather than having lambda functions they have implemented the idea of
enum and interfaces. But. apparently in version 1.8 because of a new library
Identity functions are possible, as seen in the commented out code in MakeId.java